# AGENTS.md

## 基本方針

- ユーザーの指示に従ってください。
- 重大なセキュリティ侵害、秘密情報の漏えい、大規模な削除、意図しない外部公開につながる作業は、実行前に確認してください。
- 説明や報告は日本語で行ってください。

## 作業ルール

- 変更前に関連ファイルを確認してください。
- 変更は小さく、目的に集中してください。
- 既存の実装パターン、命名、フォーマットに合わせてください。
- ファイル入出力は UTF-8 で行ってください。

## 変更後の確認

- 可能な範囲でテスト、ビルド、lint、実行確認を行ってください。
- テストは変更部分に関わるクリティカルなものを選び、1変更の通常の検証対象は既存・追加を合わせて50ケース以内に収める。50件は目標件数ではなく上限とし、文書のみ・低リスクの変更ではテストを追加・実行しない判断も可とする。パラメーター展開後のケース数で数え、無関係なケースを1テストへ詰め込んで上限を回避しない。
- 作業過程で実際に問題が発生した場合、その再現・原因切り分け・修正確認に必要な追加テストや再実行は、通常の50ケース上限とは別に数える。上限到達を理由に修正確認を省略しない。問題に直接関係する範囲で実施し、報告では通常の検証と問題対応の確認を区別する。
- テストの追加・選定には、再発防止する具体的な不具合、データ欠損、認証・公開範囲、外部連携の重大な失敗などの根拠を持たせる。件数や網羅率を増やすこと自体を目的にせず、実装の写し・重複・軽微な差分だけのテストは増やさず、変更範囲で不要と判断できるものは削減する。

## ドキュメント

- ドキュメントは `docs/` 配下に置いてください。
- 仕様や設計は `docs/specification.md`、運用手順は `docs/operations.md` を基本にしてください。
- 仕様変更、画面変更、API変更、データ構造変更、外部連携変更、デプロイ手順変更をした場合は、必ず `docs/specification.md` も同じコミットで更新してください。
- `docs/specification.md` がない場合は作成してください。

## Git

- 変更があった場合は、必要に応じて意味のある単位で `git commit` してください。
- 作業開始時に `git status` を確認し、既存の未コミット変更を不用意に上書きしないでください。

## 思考と判断の質

- 重要または後戻りしにくい判断では、一段引いて、目的・前提・範囲・長期的・全体的な影響を批判的に見直し、確認できた事実と制約から根本の目的や原因を捉える。

- 結論や完了の前に、タスクに応じて見落とし・反例・失敗条件・隠れたコスト・別の有力な解釈を検討し、自ら の判断と成果を敵対的に検証する


## プロジェクト固有メモ

- セットアップ: `[必要なら記入]`
- 開発起動: `[必要なら記入]`
- テスト: `[必要なら記入]`
- ビルド: `[必要なら記入]`
- デプロイ: `[必要なら記入]`
